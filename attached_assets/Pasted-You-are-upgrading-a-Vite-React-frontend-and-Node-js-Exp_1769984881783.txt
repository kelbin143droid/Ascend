You are upgrading a Vite + React (frontend) and Node.js + Express + PostgreSQL + Drizzle (backend) application.

Your task:

1. Create proper backend folder structure (if missing).
2. Add a weekly analytics route.
3. Wire it into Express.
4. Do NOT modify any frontend UI or Sectograph components.
5. Do NOT refactor existing code unnecessarily.

---

## STEP 1 — CREATE FOLDER STRUCTURE

If not already present, create:

/server
/routes
/db
/schema

Do not duplicate folders if they already exist.

---

## STEP 2 — CREATE ANALYTICS ROUTE

Create file:

/server/routes/analytics.ts

Implement:

GET /api/weekly-analytics?week_start_date=YYYY-MM-DD

This route must:

1. Validate week_start_date query param.
2. Calculate total time per role.
3. Calculate total time per quadrant.
4. Calculate Q2 percentage.
5. Calculate completion rate per role.

Use Drizzle ORM.
Assume tasks table contains:

* roleId
* weeklyGoalId
* quadrant
* startTime
* endTime

Assume weeklyGoals contains:

* id
* roleId
* completed
* weekStartDate

Duration calculation:
Use PostgreSQL:
EXTRACT(EPOCH FROM (end_time - start_time)) / 60

Return JSON:

{
totalTimePerRole,
totalTimePerQuadrant,
q2Percentage,
completionRatePerRole
}

Handle errors properly with try/catch.

---

## STEP 3 — REGISTER ROUTE

Locate main Express server file (server.ts, index.ts, or app.ts).

Import analytics route and register:

app.use("/api", analyticsRouter);

Ensure no existing routes are broken.

---

## STEP 4 — CLEAN CODE REQUIREMENTS

* Use async/await.
* Use proper Drizzle query syntax.
* Use grouping and aggregation in SQL.
* Do not alter existing UI code.
* Do not modify Sectograph components.
* Do not rename existing tables.

---

## IMPORTANT

If tables or schema imports are missing, reference existing schema files instead of creating duplicates.

Do not overwrite existing database schema unless required.

Focus only on backend analytics implementation.

After implementation, summarize:

* Files created
* Files modified
* New endpoint available

Do not touch frontend styling or layout.
