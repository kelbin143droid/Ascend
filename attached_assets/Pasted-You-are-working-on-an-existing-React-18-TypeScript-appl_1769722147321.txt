You are working on an existing React 18 + TypeScript application built with Vite for the frontend and Express for the backend.
The backend uses PostgreSQL with Drizzle ORM, and the frontend uses React Query for data fetching and caching.
📦 Current Architecture
Frontend: React 18 + TypeScript + Vite
Backend: Express
Database: PostgreSQL via Drizzle ORM
Data persistence: Player data is stored in a players table
Player stats are stored as a JSON column in the database
Player data is accessed via /api/player/:id
React Query caches and mutates player data on the frontend
🔒 STEP 1 — Canonical Stat Model (IMPORTANT)
The final stat model must be exactly:
Copy code
Ts
{
  strength: number
  agility: number
  sense: number
  vitality: number
}
❌ Remove intelligence completely from:
shared/schema.ts
Any backend logic
Any frontend UI or types
Update database schema and TypeScript types accordingly.
🧠 STEP 2 — Derived Stats System (Backend Authority)
Create a new server-side module:
Copy code

server/gameLogic/stats.ts
Implement a function:
Copy code
Ts
calculateDerivedStats(stats)
It should return:
xpMultiplier = 1 + sense * 0.02
staminaMax = 100 + vitality * 5
streakForgiveness = floor(agility / 10)
powerRating = strength * 1.5
Attach these derived stats to the player object returned by /api/player/:id.
⚡ STEP 3 — Server-Enforced Stat Allocation
Stat increases must:
Be validated on the server
Require available stat points
Permanently update the database
Reject requests with no points remaining
Implement stat allocation logic in server/storage.ts.
🔁 STEP 4 — Frontend Stat Button Wiring
The “+” buttons on the Stats screen must:
Call a backend mutation endpoint
Use React Query mutations
Optimistically update the UI
Roll back on error
The server is the single source of truth.
🧮 STEP 5 — XP Formula System
Create:
Copy code

server/gameLogic/xp.ts
Implement:
Copy code
Ts
calculateXP({ baseXP, stats, derived })
XP must scale using derived.xpMultiplier.
🧩 STEP 6 — Minimal Action System (ONE per stat)
Implement exactly one real action per stat:
Strength → Workout timer (≥10 min)
Sense → Focus timer (single-task)
Vitality → Daily recovery check-in
Agility → Streak protection logic
These actions should:
Award XP using the XP formula
Update the player state persistently
Do NOT add extra features or monetization.
🧠 STEP 7 — System Messages (Lightweight)
Add simple system messages such as:
“Sense increased → XP efficiency +2%”
“Vitality low → stamina penalty applied”
“Streak preserved by Agility bonus”
These can be returned with API responses or logged server-side.
🎯 Constraints
Keep implementation minimal and clean
No AI features
No feature bloat
Server is always authoritative
All logic must be deterministic and testable
Proceed step-by-step and explain each change briefly as you implement it.