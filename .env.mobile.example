# Copy to `.env.mobile` (or `.env.production.local` when running `vite build`)
# before building the mobile bundle. Without this, the WebView has no backend
# to talk to.
VITE_API_BASE_URL=https://your-deployed-backend.example.com
